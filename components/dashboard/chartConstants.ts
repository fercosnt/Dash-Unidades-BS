/** Largura do eixo Y nos gráficos do dashboard (Faturamento e Evolução). Igual nos dois para os eixos Y ficarem alinhados. */
export const CHART_Y_AXIS_WIDTH = 30;

/** Margem esquerda do gráfico (área de plot). Zero para o conteúdo ficar junto ao eixo Y. */
export const CHART_MARGIN_LEFT = 0;

/** Margem inferior do gráfico (espaço para o eixo X). Igual nos dois para a linha X ficar alinhada. */
export const CHART_MARGIN_BOTTOM = 2;

/** Margem direita do gráfico (espaço à direita da área de plot). */
export const CHART_MARGIN_RIGHT = 11;

/** Padding esquerdo do eixo X (área de plot). Zero para o conteúdo ficar junto ao eixo X. */
export const CHART_X_PADDING_LEFT = 0;

/** Padding direito do eixo X no gráfico de barras (Faturamento). */
export const CHART_X_PADDING_RIGHT_BAR = 0;

/** Padding direito do eixo X no gráfico de linha (Evolução), para o último rótulo não ser cortado. */
export const CHART_X_PADDING_RIGHT_LINE = 10;

/** Altura reservada para os rótulos do eixo X (usado no overlay para não cobri-los). */
export const CHART_X_LABEL_AREA_HEIGHT = 28;
